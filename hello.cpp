#include<iostream> // Including the input/output stream library to enable input and output operations.

using namespace std; // Using the standard namespace to avoid having to prepend 'std::' to standard C++ objects and functions.

int main (){ // The starting point of the program where execution begins.

    std::cout << "Hello DSA" << std::endl; // Outputting the string "Hello DSA" followed by a newline character to the console.

} // End of the main function.
