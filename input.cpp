#include<iostream>  // Includes the input/output stream library

using namespace std;  // Specifies that we are using the standard namespace

int main(){  // Main function where the program execution begins

    int s;  // Declares an integer variable named s

    std::cin >> s;  // Takes input from the user and stores it in the variable s

    std::cout << s << std::endl;  // Outputs the value stored in s followed by a newline

    return 0;  // Indicates successful termination of the program
}
